# Face Detection using OpenCV

This project detects human faces using Python and OpenCV.

## Technologies Used
- Python
- OpenCV
- Computer Vision

## How it works
The program uses a pre-trained Haar Cascade classifier to detect faces from a webcam video stream.

## How to Run
1. Install OpenCV
2. Run the Python script
3. Webcam will detect faces in real-time
